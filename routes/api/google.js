/* const router          = require('express').Router();
const googleConfig    = require('../../config/googleConfig');

const albumId = googleConfig.albumId;


/* router.get('/google', async (req, res) => {
   try  {
    const getAll = await google.findAll();
    return res.send(getAll);
   } catch (err) {
       console.error(err);
       return res.status(500).json({ msg: 'Server failure. Unable to quary library API' });
   }
}); 

router.get('/google', async (req, res) => {

    try {
        const 
    } catch (err) {
        console.error(err);
        return res.status(500).send({ msg: `Server failure, album not found` });
    }

})

module.exports = router; */