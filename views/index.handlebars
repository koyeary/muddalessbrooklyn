<body>
    <div class="my-3 mx-auto header">
        {{> header }}
        {{> mobileNav }}
    </div>
    <section class="d-none d-md-block">
        {{> carousel }}
    </section>
    <section class="d-md-none d-xs-block">
        {{> mobileCarousel }}
    </section>
    <section>
        {{> nav }}
    </section>

    <main>
        <div class="about" id="about">
            {{> about }}
        </div>
        <div class="contact">
            <div class="row mx-auto py-5" id="contact"></div>
            <div class="row mx-auto py-5 justify-content-center">
                {{> contact}}
            </div>
            <div class="row mx-auto py-5"></div>
        </div>
    </main>
    <section>
        {{> footer }}
    </section>
</body>